extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    div(class="message")
      div= message.user
      div= message.text
      div= message.added
  a(href="/new") Add Message